{
  "name": "tsp-rotation",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://Curiousjp40.github.io/tsp-rotation",
  "dependencies": {
    "papaparse": "^5.4.1",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "react-scripts": "5.0.1",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "fetch-prices": "node scripts/fetch-prices.js",
    "backtest": "node scripts/backtest.js",
    "sweep": "node scripts/sweep.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
